<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xuyiyang.management.dao.testModule.TestMapper">
    <select id="selectOne"  parameterType="int" resultType="test">
         select id,company_name as companyName,channel_name as channelName from bd_channel where id = #{id}
    </select>
</mapper>